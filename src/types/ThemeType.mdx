import { Meta } from '@storybook/blocks';

<Meta title="Type/ThemeType" />

# ThemeType

`ThemeType` is a type used in [<ins>useTheme()</ins>](/docs/hook-usetheme--docs) hook.

```tsx
export type ThemeType = 'light' | 'dark';
```
